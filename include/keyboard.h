#ifndef KEYBOARD_H
#define KEYBOARD_H

void WIN_ReadKeys(int INPUT) {	//0=none; 1=Keydown; 2=keyup
	
	if (INPUT = 1) {
		if (event.key.code == sfKeyLeft) {
			STATUS = "Left";
		}
	
	
	}
	
	if (INPUT = 2) {
		
		
		
	}
}

#endif //KEYBOARD_H